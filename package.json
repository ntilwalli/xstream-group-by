{
  "name": "xstream-group-by",
  "version": "1.0.0",
  "description": "An xstream operator to emit streams grouped by requested key",
  "main": "index.js",
  "typings": "index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ntilwalli/xstream-group-by.git"
  },
  "scripts": {
    "compile": "tsc",
    "prepublishOnly": "npm run compile",
    "doctest": "markdown-doctest",
    "premocha": "npm run compile",
    "mocha": "mocha test.ts --require ts-node/register",
    "test": "npm run mocha && npm run doctest"
  },
  "author": "Nikhil Tilwalli",
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "5.2.x",
    "@types/node": "^8.0.30",
    "markdown-doctest": "0.9.1",
    "mocha": "5.2.x",
    "ts-node": "6.1.x",
    "typescript": "2.5.x"
  },
  "dependencies": {
    "xstream": ">=6.6.0"
  }
}
